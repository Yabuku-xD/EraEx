# Core web app / data handling
flask>=3.0
python-dotenv>=1.0
numpy>=1.26,<3
pandas>=2.0
tqdm>=4.66
scikit-learn>=1.3

# Search / embedding / ML stack
torch>=2.6
transformers>=4.40
accelerate>=0.30
sentence-transformers>=3.0
faiss-cpu
scipy>=1.10

# Audio + media metadata enrichment
librosa>=0.10
soundfile>=0.12
# Keep yt-dlp on latest mainline because extractor behavior changes frequently.
yt-dlp @ git+https://github.com/yt-dlp/yt-dlp.git

# Test tooling
pytest>=8.0

# Notebook / analysis extras (used by notebooks/)
ipykernel>=6
matplotlib>=3.8
seaborn>=0.13
wordcloud>=1.9

# Optional local lyrics sidecar service (EraEx can run without this package; app uses HTTP fallback)
# Lyrica @ git+https://github.com/Wilooper/Lyrica.git
